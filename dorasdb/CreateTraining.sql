CREATE TABLE Training(
    tid int PRIMARYKEY AUTO_INCREMENT NOT NULL,
    name varchar(50) NOT NULL default'Under work',
    type varchar(255),
    link varchar(89),
    description varchar(510) DEFAULT ' '
)