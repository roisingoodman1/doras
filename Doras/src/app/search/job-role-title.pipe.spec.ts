import { JobRoleTitlePipe } from './job-role-title.pipe';

describe('JobRoleTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new JobRoleTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
